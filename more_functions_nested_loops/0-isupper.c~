#include "main.h"

/**
*_isupper - checks for uppercase character
*@c: uppercase character
*Return: 1 if uppercase and 0 otherwise
*/

int _isupper(int c)
{
char ch;

if (ch >= 'A' && ch <= 'Z')
{
return (1);
}
else
{
return (0);
}

}
