#include "main.h"
#include <stdio.h>
#include <string.h>

/**
*
*
*
*/

void puts_half(char *str)
{
  int a, b, s;
s = strlen(str);
printf("%d\n", s);

b = s / 2;
a = b;
for (b = a; b <= s; b++)
{
  printf("%c", str[b]);
}
 printf("\n");
}
